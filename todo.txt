+ create sql component and test sqlite_manager from frontend
+ organize frontend code into seperate modules for logic and view


- better command errors
- remember last paths in dialogs (localstorage keyd by title?)
- print debug mesages as events to frontend

+ Access
    + create interface for accual mdb [raczej nigdy się nie stanie]
    + import desired db structure form mdb
    + load decimal extension [XD]
    + fix decimal check
    + create views?
    + implement import from mdb
        + mdb export macro
        + sanitize resulting csv
        + test reimport
        + invalid ł conversion
        + missing spaces
        + , into . inside quotes
        + missing rows in zlecenia naprawy
            + MDB Memo type is truncated to 255 chars, should be 65535
        + use better csv extension
        + deal with numbers
        + csv import
    + implement export to mdb (csv)
        + csv export
            + quotes on numbers
            + do only OEM, without utf
            + remove milis from date
        + mdb import macro
    + test decimals in csv in access (mniejsze niż 922337203685477,5808 [922 337 203 685 477] ), za duże się nie wpisują w ogóle
    - float and double constrains

- Buckups API
    - local and manual
    - periodic
    - remote
    - progress

- Dynamic Forms API

- Core
    + context menu
    - create main UI layout
        - icons
        - toolbar
        - footbar
        - prompts and alerts
            - content
            - custom inputs
            - custom buttons
        - messages (popups)
            - history
                - filtering
            + popup layout
            + corner messages
                + auto close
                - full view
                + custom onclick
            + types
                + info
                + warn
                + error
                + critical error
        - errors messages
            - sql update constraint
            - system error
        - main area
    - Query Viewer
        + basic view
        + scroller
        + advanced sorting
        + search
            - highlight
        - update
        - insert
        - automate form integration
        - fix scrolling back to 0, when changing search fields
    - Forms API
        - Dataset and Forms
            + standerd form value
            + table update manager
            + automated update logic
            + automated fetch logic
            + update only changed values
            + add transaction for update
            + better integrtation with scroller
            + compact interface for src_query bindings
            + aux queries
            + automate form validation
            + insert
            + transactions for batch operations
            - automatic scroller query generation
            - sub datasets
            - delete
            - detect and dont display empty results
        + Query Form Scroller
            + working scroller
            + Simple Mode
            + toggle for simple
            - better indicators for empty/ood states
            - (BUG) alert box not showing, when scrolling by step while on bound
        - Kontrolki
            + custom context menu
                - make "Zaznacz Wszystko" on read-only inputs work
            - Labels
            + Input
                + Integer
                + Number
                - float
                + Decimal
                + Text
                + Datetime
                    - allow format YY-MM-DD (płace -> data)
                + Input constrains
                    + min/max
                    + length
                    - presets for different-sized fields from access
                + tooltips for constraint violation
                + readonly
                - optional comma as separator
                + null
                    + better null handling
                    - add CTRL+Z history when clearing a field
                + hints
                    - make hints better
            + Enum (select)
                - advanced value modification
            + Query Enum
            - Bool (checkbox)
            - Radiobox
            - Frame
            + Field-Search

    - create toolbars

- Query Builder ?
- query whitespace remover


+ WinBox
    + core (open, focus, close)
    + mounting components
    + fix viewport update when resize
    - parent windows

